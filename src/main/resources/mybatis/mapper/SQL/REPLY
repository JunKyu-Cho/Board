CREATE TABLE REPLY (
 REPLY_ID bigint generated by default as identity,
 CONTENT_ID bigint,
 WRITER varchar(20),
 CONTENT varchar(2000),
 REGDATE varchar(20),
 PRIMARY KEY (REPLY_ID),
 FOREIGN KEY(CONTENT_ID) REFERENCES BOARD(ID)
)